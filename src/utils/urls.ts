import { environment } from 'src/environments/environment';
export const APP_API_URL = environment.backend;
export const APP_HD_URL = environment.hdAPI;
export const APP_HD_URL1 = environment.hdAPI2;
